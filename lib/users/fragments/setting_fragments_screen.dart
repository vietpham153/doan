import 'package:flutter/material.dart';

class settingfragmentsscreen extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Center(
        child: Text(
          "Settings"
        )
        ),
    );
  }
}